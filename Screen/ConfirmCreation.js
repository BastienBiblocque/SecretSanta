import {ScrollView, Text} from "react-native";
import * as React from "react";

export function ConfirmCreationScreen() {
    return (
        <ScrollView>
            <Text>Home</Text>
        </ScrollView>
    );
}